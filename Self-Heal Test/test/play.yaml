---
- name: Test Play
  hosts: all
  user: root
  gather_facts: no
  tasks:
    
     - name: test task
       command: echo "{{ item }}"
       with_items:
            - "{{ groups.all }}"
       register: result
     
     - debug: 
            var: result
